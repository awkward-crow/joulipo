# closed-maps

A prototype-based style where objects are plain dictionaries with data and behaviour stored together as named slots. Behaviour is implemented as closures that close over the dictionary itself.
